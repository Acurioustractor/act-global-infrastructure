Per the **FALLBACK** rule in your task spec: every funder above carries `(last: never)` — meaning no contact record exists in GHL for any of them. The rule is unambiguous: draft a single-line note rather than fabricate.

---

## QBE Foundation (null — no contact record)
**Status:** no contact record in GHL — create a contact first

## Australian Communities Foundation (null — no contact record)
**Status:** no contact record in GHL — create a contact first

## Vincent Fairfax Family Foundation (null — no contact record)
**Status:** no contact record in GHL — create a contact first

## AMP Foundation (null — no contact record)
**Status:** no contact record in GHL — create a contact first

## Centrecorp Foundation (null — no contact record)
**Status:** no contact record in GHL — create a contact first

## Nova Peris Foundation (null — no contact record)
**Status:** no contact record in GHL — create a contact first

## The Funding Network (null — no contact record)
**Status:** no contact record in GHL — create a contact first

---

## Escalation flags
All seven funders are at **null** last-touch (no record created). Before cadence drafting can apply, a GHL contact record must exist for each. No funder here crosses the >21-day escalation threshold because no touch record exists at all — this is a **record-creation problem, not a cadence problem**.