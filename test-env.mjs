import 'dotenv/config';
console.log('GITHUB_TOKEN:', process.env.GITHUB_TOKEN ? 'FOUND' : 'NOT FOUND');
console.log('GH_PROJECT_TOKEN:', process.env.GH_PROJECT_TOKEN ? 'FOUND' : 'NOT FOUND');
console.log('NOTION_TOKEN:', process.env.NOTION_TOKEN ? 'FOUND' : 'NOT FOUND');
