{
  "name": "@act/notion-workers",
  "version": "0.1.0",
  "private": true,
  "description": "Notion Workers — custom agent tools powered by ACT ecosystem data",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs",
    "check": "tsc --noEmit",
    "dev": "tsc --watch"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.9.2"
  },
  "dependencies": {
    "@notionhq/workers": ">=0.0.0",
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "@act/intel": "workspace:*",
    "@types/node": "^22.9.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.0"
  }
}
