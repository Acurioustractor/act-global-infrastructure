{
  "name": "@act/notion-workers",
  "version": "0.1.0",
  "private": true,
  "description": "Notion Workers — custom agent tools powered by ACT ecosystem data",
  "main": "dist/index.js",
  "scripts": {
    "build": "mkdir -p dist && cp bundle/index.js dist/index.js && echo 'Deployed pre-built bundle'",
    "build:local": "tsc --noEmit && node esbuild.config.mjs && cp dist/index.js bundle/index.js",
    "check": "tsc --noEmit",
    "dev": "tsc --watch"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.9.2"
  },
  "dependencies": {
    "@notionhq/workers": ">=0.0.0",
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "esbuild": "^0.25.0",
    "tsx": "^4.21.0",
    "typescript": "^5.8.0"
  }
}
